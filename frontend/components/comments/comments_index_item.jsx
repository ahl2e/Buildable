import React from 'react';

const CommentsIndexItem = (comment) => {
  debugger
  return
      <li>
        <p>{comment.title}</p>
        <p>{comment.body}</p>
        <p>{comment.user_id}</p>
      </li>
    };

export default CommentsIndexItem;
